import { OpenAPIV3 } from 'openapi-types'

import { SwaggerJsonUrlItem } from '../tools'

export function parseSwaggerJsonV3(
  swaggerJson: OpenAPIV3.Document,
  configItem: SwaggerJsonUrlItem
): SwaggerJsonTreeItem[] {
  // console.log(swaggerJson, configItem)

  return []
}
